# FBO Data Flow Architecture Diagram

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                            ProPilot FBO System                              │
└─────────────────────────────────────────────────────────────────────────────┘

                               USER OPENS AIRPORT
                                       │
                                       ▼
                          ┌────────────────────────┐
                          │   AirportDetailView    │
                          │    (FBO Tab Tapped)    │
                          └────────────────────────┘
                                       │
                                       ▼
                          ┌────────────────────────┐
                          │  loadFBOs() called     │
                          │  Load cached data →    │
                          └────────────────────────┘
                                       │
                                       ▼
                          ┌────────────────────────────────────────┐
                          │   AirportDatabaseManager.getFBOs()     │
                          │   Returns local cache (CSV + previous  │
                          │   CloudKit data)                       │
                          └────────────────────────────────────────┘
                                       │
                                       ▼
                          ┌────────────────────────┐
                          │  Display FBOs to user  │
                          │  (Instant, no wait!)   │
                          └────────────────────────┘
                                       │
                          ┌────────────┴────────────┐
                          │                         │
                          ▼                         ▼
              ┌────────────────────┐    ┌──────────────────────┐
              │  loadFBOsAsync()   │    │  User sees FBOs      │
              │  Background fetch  │    │  immediately         │
              │  from CloudKit     │    │  ✓ Offline works!    │
              └────────────────────┘    └──────────────────────┘
                          │
                          ▼
              ┌────────────────────────────────────┐
              │  CloudKit Public Database          │
              │  Query: airportCode == "KSFO"      │
              │  Returns user-contributed FBOs     │
              └────────────────────────────────────┘
                          │
                          ▼
              ┌────────────────────────────────────┐
              │  Smart Merge Algorithm             │
              │  mergeFBOData(local:cloud:)        │
              └────────────────────────────────────┘
                          │
        ┌─────────────────┴─────────────────┐
        │                                   │
        ▼                                   ▼
┌──────────────────┐              ┌──────────────────┐
│  Local FBOs      │              │  CloudKit FBOs   │
│  (CSV baseline   │    MERGE     │  (User updates)  │
│   + cached)      │◄────────────►│                  │
└──────────────────┘              └──────────────────┘
        │                                   │
        │          Merge Rules:             │
        │  1. Match by normalized name      │
        │  2. Newest fuel prices win        │
        │  3. Amenities use OR logic        │
        │  4. Keep verification status      │
        │  5. Preserve CloudKit IDs         │
        │                                   │
        └─────────────┬─────────────────────┘
                      │
                      ▼
        ┌─────────────────────────────────┐
        │  Merged FBO List                │
        │  • CSV verified baseline        │
        │  • + User contributions         │
        │  • + Fresh fuel prices          │
        │  • + Community amenities        │
        └─────────────────────────────────┘
                      │
                      ▼
        ┌─────────────────────────────────┐
        │  Cache locally (UserDefaults)   │
        │  For next launch                │
        └─────────────────────────────────┘
                      │
                      ▼
        ┌─────────────────────────────────┐
        │  Update UI                      │
        │  Display merged FBO list        │
        │  with data source badges        │
        └─────────────────────────────────┘
```

---

## Data Source Layers

```
┌───────────────────────────────────────────────────────────────────────┐
│  LAYER 3: User View (What pilot sees)                                │
│  ───────────────────────────────────────────────────────────────────  │
│  • Signature Aviation                           [Verified] [Baseline] │
│  • Atlantic Aviation                            [Community Updated]   │
│  • Million Air                                  [Verified] [Baseline] │
└───────────────────────────────────────────────────────────────────────┘
                                  ▲
                                  │ (Smart Merge)
                                  │
        ┌─────────────────────────┴─────────────────────────┐
        │                                                   │
┌───────▼─────────────────────────┐      ┌────────────────▼──────────────────┐
│  LAYER 1: CSV Baseline          │      │  LAYER 2: CloudKit Crowdsourced   │
│  ─────────────────────────────  │      │  ───────────────────────────────  │
│  • Bundled with app             │      │  • User additions                 │
│  • Works offline                │      │  • Fuel price updates             │
│  • Verified & curated           │      │  • Community ratings              │
│  • Updated via app releases     │      │  • Real-time sync                 │
│  • 164 FBOs, 82 airports        │      │  • Cross-device                   │
│                                 │      │                                   │
│  Source: propilot_fbos.csv      │      │  Source: CloudKit Public DB       │
│  Version: 2                     │      │  Record Type: CrowdsourcedFBO     │
└─────────────────────────────────┘      └───────────────────────────────────┘
```

---

## Fuel Price Update Flow

```
        USER TAPS "UPDATE FUEL PRICE"
                     │
                     ▼
        ┌────────────────────────────┐
        │  QuickFuelUpdateSheet      │
        │  User enters:              │
        │  • Jet A: $7.25            │
        │  • AvGas: $8.50            │
        └────────────────────────────┘
                     │
                     ▼
        ┌────────────────────────────────────────┐
        │  airportManager.updateFuelPrice()      │
        │  • Sets fuelPriceDate = now            │
        │  • Sets fuelPriceReporter = user ID    │
        └────────────────────────────────────────┘
                     │
        ┌────────────┴────────────┐
        │                         │
        ▼                         ▼
┌──────────────────┐    ┌──────────────────┐
│  Update CloudKit │    │  Update Local    │
│  Public DB       │    │  Cache           │
└──────────────────┘    └──────────────────┘
        │                         │
        └────────────┬────────────┘
                     │
                     ▼
        ┌────────────────────────────┐
        │  Reload FBOs               │
        │  (triggers merge again)    │
        └────────────────────────────┘
                     │
                     ▼
        ┌────────────────────────────────┐
        │  Other Users See Update        │
        │  On next CloudKit fetch        │
        │  Fuel price automatically wins │
        │  (newest date takes priority)  │
        └────────────────────────────────┘
```

---

## Merge Algorithm Detail

```
INPUT:
  local = [CSV FBOs + Cached CloudKit FBOs]
  cloud = [Fresh CloudKit FBOs]

STEP 1: Normalize names for matching
  "Signature Aviation" → "signature"
  "Signature Aviation FBO" → "signature"
  "Signature" → "signature"
  → All match!

STEP 2: Build merged dictionary
  For each local FBO:
    merged[normalized_name] = local_fbo

STEP 3: Merge CloudKit data
  For each cloud FBO:
    If duplicate (normalized name matches):
      ┌─────────────────────────────────────────────┐
      │  Keep CloudKit ID (for sync)                │
      │  Use CloudKit contact info (if user-updated)│
      │  OR amenities (either true = true)          │
      │  Newest fuel price wins (by date)           │
      │  Keep verified status from either           │
      │  Update timestamp to newest                 │
      └─────────────────────────────────────────────┘
    Else (new FBO):
      Add cloud FBO to merged

STEP 4: Sort by name
  Return sorted(merged.values)

OUTPUT:
  merged = [Combined FBO list with best data]
```

---

## Version Update Flow

```
DEVELOPER UPDATES CSV
        │
        ├─ Edit propilot_fbos.csv
        ├─ Add new FBOs
        ├─ Update fuel prices
        ├─ Fix errors
        │
        ▼
INCREMENT VERSION
        │
        ├─ Open AirportDatabaseManager.swift
        ├─ Change: currentFBOCSVVersion = 3
        │
        ▼
RELEASE APP UPDATE
        │
        ▼
USER UPDATES APP
        │
        ▼
┌───────────────────────────────┐
│  First Launch After Update    │
│  ────────────────────────────  │
│  1. Check version:             │
│     loadedVersion = 2 (old)    │
│     currentVersion = 3 (new)   │
│                                │
│  2. Reload CSV:                │
│     Load all FBOs from v3 CSV  │
│                                │
│  3. Merge with CloudKit:       │
│     User's previous CloudKit   │
│     contributions preserved    │
│                                │
│  4. Save version:              │
│     loadedVersion = 3          │
│                                │
│  5. Cache merged data          │
└───────────────────────────────┘
        │
        ▼
USER SEES UPDATED DATA
  • New FBOs from CSV
  • Updated baseline prices
  • Their CloudKit contributions
  • Other users' updates
```

---

## Data Source Badges

```
┌──────────────────────────────────────────────────────────┐
│  Signature Aviation                                      │
│  ───────────────────────────────────────────────────────  │
│  ✓ Verified  📄 Baseline Data                            │
│                                                          │
│  ↑ FBO in CSV, marked as verified                       │
└──────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────┐
│  Atlantic Aviation                                       │
│  ───────────────────────────────────────────────────────  │
│  ✓ Verified  👥 Community Updated                        │
│                                                          │
│  ↑ FBO in CSV, but user updated it via CloudKit         │
└──────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────┐
│  Joe's FBO                                               │
│  ───────────────────────────────────────────────────────  │
│  👥 Community Updated                                    │
│                                                          │
│  ↑ User-added FBO (not in CSV)                          │
└──────────────────────────────────────────────────────────┘
```

---

## Error Handling

```
┌─────────────────────────────────────┐
│  Scenario: CloudKit Unavailable    │
└─────────────────────────────────────┘
         │
         ├─ No internet
         ├─ CloudKit down
         ├─ Not signed in
         │
         ▼
    Fetch fails
         │
         ▼
┌─────────────────────────────────────┐
│  Keep cached data                   │
│  • CSV baseline still works         │
│  • Previous CloudKit data cached    │
│  • User sees FBOs (offline)         │
│  • Try again on next launch         │
└─────────────────────────────────────┘

Result: App works offline! ✓
```

```
┌─────────────────────────────────────┐
│  Scenario: User Adds Duplicate     │
└─────────────────────────────────────┘
         │
         ├─ User adds "Signature Aviation"
         ├─ Already in CSV
         │
         ▼
    Save to CloudKit
         │
         ▼
    Next merge detects duplicate
         │
         ▼
┌─────────────────────────────────────┐
│  Smart merge combines:              │
│  • CSV verified status              │
│  • User's CloudKit ID               │
│  • User's updated contact info      │
│  • Newest fuel prices               │
└─────────────────────────────────────┘

Result: Single FBO, best data! ✓
```

---

## Performance Optimization

```
┌──────────────────────────────────────────────────────────┐
│  LOAD STRATEGY: Instant Display, Background Sync        │
└──────────────────────────────────────────────────────────┘

Timeline:
  t=0ms    User taps FBO tab
  t=10ms   Load cached FBOs → DISPLAY IMMEDIATELY
  t=50ms   Start CloudKit fetch (background)
  t=500ms  CloudKit results arrive
  t=520ms  Merge completes
  t=530ms  UI updates (smooth transition)

User Experience:
  • Instant: FBOs appear immediately (cached)
  • Smooth: CloudKit update happens invisibly
  • Reliable: Works offline
  • Fresh: Gets updates when online

Technical:
  • Main thread: Load cache only (fast!)
  • Background thread: CloudKit fetch
  • Smart merge: Preserves local data
  • UI update: One batch update
```

---

## Summary

**Architecture:** 3-tier hybrid (CSV + CloudKit + Cache)  
**Load Time:** Instant (cached) + background sync  
**Offline:** Fully functional with CSV baseline  
**Data Quality:** CSV verified + community contributions  
**Merge Strategy:** Smart, non-destructive, newest wins  
**User Experience:** Offline-first, community-powered

**Key Insight:**
> CSV provides reliable baseline, CloudKit enables real-time updates,
> smart merge combines the best of both worlds.
